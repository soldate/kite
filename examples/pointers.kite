type node {
    int value;
    pointer node next;

    void init(int v) {
        value = v;
        next = 0;
    }
}

special type main {
    void main() {
        pointer int p = 0;
        stack node n = node(10);

        p = p + 1;
    }
}
