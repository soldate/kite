type node {
    int value;

    void init(int v) {
        value = v;
    }
}

special type memory {
    list heap_objects;

    pointer on_heap(int size, int type) {
        pointer p = allocator.alloc(size);

        if (type == node.id) {
            heap_objects.add(p);
        }

        return p;
    }

    void clean_heap() {
        heap_objects.delete_all();
    }
}

special type main {
    void main() {
        node a = node(10);
        node b = node(20);

        a.value = b.value;

        memory.clean_heap();
    }
}
